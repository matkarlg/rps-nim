sudo: required
language: c
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get install -y nim
script:
  - nim c --cc:$CC --verbosity:2 src/rps_nim.nim
addons:
  apt:
    update: true
notifications:
  email: false
